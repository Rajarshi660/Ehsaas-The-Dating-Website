<nav class="sticky top-0 z-[100] bg-black/60 backdrop-blur-xl border-b border-white/10 px-6 py-4">
    <div class="max-w-5xl mx-auto flex justify-between items-center">
        <a href="/explore" class="text-[10px] font-black uppercase tracking-widest text-gray-400 hover:text-white transition-all">Explore</a>

        <div class="flex flex-col items-center cursor-pointer" onclick="window.location.href='/profile'">
            <h1 class="text-xl font-black text-white tracking-tighter">EHSAAS</h1>
        </div>

        <div class="flex items-center gap-5">
            <% if (locals.currentUser) { %>
                <button onclick="openModal('matchRequestModal')" class="relative text-gray-400 hover:text-rose-500 transition-all">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l8.84-8.84 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"></path></svg>
                    <% if (locals.pendingCount > 0) { %><span class="absolute -top-1 -right-1 w-2 h-2 bg-rose-500 rounded-full border border-black animate-pulse"></span><% } %>
                </button>

                <button onclick="openModal('chatListModal')" class="text-gray-400 hover:text-blue-400 transition-all">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path></svg>
                </button>

                <button onclick="openModal('uploadModal')" class="w-8 h-8 rounded-full bg-white/10 flex items-center justify-center text-white hover:bg-rose-500 transition-all font-bold">+</button>

                <a href="/logout" class="text-[9px] font-black uppercase text-rose-500 border border-rose-500/20 px-3 py-1 rounded-full hover:bg-rose-500 hover:text-white transition-all">LOGOUT</a>
            <% } %>
        </div>
    </div>
</nav>