<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Explore | Ehsaas</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .vibe-card { transition: all 0.4s ease; }
        .vibe-card:hover { transform: translateY(-8px); box-shadow: 0 20px 40px rgba(225, 29, 72, 0.15); }
    </style>
</head>
<body class="bg-[#050505] text-white min-h-screen font-sans">

    <nav class="sticky top-0 z-50 bg-black/80 backdrop-blur-xl border-b border-white/5 px-8 py-4 flex justify-between items-center">
        <a href="/profile" class="text-rose-500 font-black text-2xl tracking-tighter italic">EHSAAS</a>
        <div class="flex gap-8 items-center text-sm font-bold">
            <a href="/profile" class="hover:text-rose-500 transition">MY PROFILE</a>
            <a href="/logout" class="bg-rose-600/10 text-rose-500 px-4 py-2 rounded-full hover:bg-rose-600 hover:text-white transition">LOGOUT</a>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto py-16 px-6">
        <header class="mb-12">
            <h1 class="text-5xl font-black tracking-tight mb-4">Discover Your <span class="text-rose-500">Vibe.</span></h1>
            <p class="text-gray-400 text-lg">Curated matches based on your unique interests.</p>
        </header>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% if (matches.length > 0) { %>
                <% matches.forEach(match => { %>
                    <div class="vibe-card bg-[#111] rounded-[2.5rem] overflow-hidden border border-white/10 flex flex-col">
                        <div class="h-80 relative">
                            <img src="<%= match.profilePic || 'https://via.placeholder.com/400' %>" class="w-full h-full object-cover">
                            <div class="absolute inset-0 bg-gradient-to-t from-[#111] via-transparent to-transparent"></div>
                            <div class="absolute top-6 right-6 bg-black/60 backdrop-blur-md px-4 py-2 rounded-2xl border border-white/20">
                                <span class="text-rose-500 font-black text-sm"><%= match.percent %>% Match</span>
                            </div>
                        </div>

                        <div class="p-8 pt-0 flex-1 flex flex-col">
                            <h3 class="text-2xl font-bold mb-2"><%= match.name %></h3>
                            <p class="text-gray-400 text-sm mb-6 line-clamp-2"><%= match.bio || 'No bio shared yet.' %></p>

                            <div class="mt-auto">
                                <h4 class="text-[10px] uppercase tracking-widest font-black text-rose-500 mb-3">Shared Vibes</h4>
                                <div class="flex flex-wrap gap-2 mb-8">
                                    <% if (match.common && match.common.length > 0) { %>
                                        <% match.common.forEach(genre => { %>
                                            <span class="text-[10px] bg-white/5 border border-white/5 px-3 py-1.5 rounded-lg text-gray-300">
                                                #<%= genre %>
                                            </span>
                                        <% }) %>
                                    <% } else { %>
                                        <span class="text-[10px] text-gray-600 italic">Exploring new territory...</span>
                                    <% } %>
                                </div>

                                <a href="/profile/<%= match._id %>" class="block w-full text-center bg-white text-black py-4 rounded-2xl font-black text-xs uppercase tracking-widest hover:bg-rose-500 hover:text-white transition-all shadow-xl">
                                    Connect
                                </a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full py-32 text-center border-2 border-dashed border-white/10 rounded-[3rem]">
                    <p class="text-gray-500 text-xl font-medium mb-4">No vibe matches currently in your area.</p>
                    <a href="/profile" class="text-rose-500 font-bold hover:underline">Update your genres to see more</a>
                </div>
            <% } %>
        </div>
    </main>

</body>
</html>