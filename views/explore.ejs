<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Ehsaas | Explore Vibes</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/style.css">
</head>
<body class="gradient-bg min-h-screen text-white">

    <nav class="w-full p-5 flex justify-between items-center bg-black/20 backdrop-blur-xl sticky top-0 z-50 border-b border-white/5">
        <div class="flex items-center gap-3">
            <div class="w-10 h-10 bg-rose-600 rounded-lg flex items-center justify-center font-bold">E</div>
            <span class="text-xl font-black tracking-tighter">EHSAAS</span>
        </div>

        <div class="flex items-center gap-4">
            <a href="/profile" class="flex items-center gap-3 bg-white/5 hover:bg-white/10 p-1 pr-4 rounded-full transition border border-white/10">
                <div class="w-8 h-8 rounded-full bg-gradient-to-tr from-rose-500 to-purple-600 flex items-center justify-center text-xs font-bold">
                    <%= currentUser.name.charAt(0) %>
                </div>
                <span class="text-sm font-bold">My Profile</span>
            </a>
        </div>
    </nav>

    <main class="max-w-6xl mx-auto p-8">
        <h2 class="text-3xl font-black mb-8">People Vibing with You <span class="text-rose-500">.</span></h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% matches.forEach(match => { %>
                <div class="auth-card p-6 group hover:scale-[1.02] transition-transform">
                    <div class="flex justify-between items-start mb-4">
                        <div class="w-16 h-16 bg-white/10 rounded-2xl flex items-center justify-center text-2xl font-bold">
                            <%= match.name.charAt(0) %>
                        </div>
                        <span class="bg-rose-500/20 text-rose-400 text-xs font-bold px-3 py-1 rounded-full border border-rose-500/30">
                            <%= match.matchPercent %>% Match
                        </span>
                    </div>
                    <h3 class="text-xl font-bold"><%= match.name %>, <%= match.age %></h3>
                    <p class="text-slate-400 text-sm mb-4">"<%= match.bio || 'Feeling the vibe on Ehsaas' %>"</p>
                    
                    <div class="flex flex-wrap gap-2">
                        <% match.commonGenres.forEach(genre => { %>
                            <span class="text-[10px] uppercase tracking-widest bg-white/5 px-2 py-1 rounded-md text-slate-300">
                                #<%= genre %>
                            </span>
                        <% }) %>
                    </div>
                </div>
            <% }) %>
        </div>
    </main>

</body>
</html>