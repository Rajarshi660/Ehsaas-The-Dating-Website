<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Join Ehsaas</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen">
    <div class="w-full max-w-md p-8 bg-[#111] rounded-[2.5rem] border border-white/5 shadow-2xl">
        <h2 class="text-3xl font-black text-center mb-8">Create Your Vibe</h2>
        <form action="/auth/signup" method="POST" class="space-y-4">
            <input type="text" name="name" placeholder="Full Name" class="w-full bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500" required>
            <input type="email" name="email" placeholder="Email" class="w-full bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500" required>
            <input type="password" name="password" placeholder="Password" class="w-full bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500" required>
            
            <div class="grid grid-cols-2 gap-4">
                <select name="gender" class="bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500" required>
                    <option value="" disabled selected>My Gender</option>
                    <option value="male">Male</option>
                    <option value="female">Female</option>
                </select>
                <select name="interestedIn" class="bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500" required>
                    <option value="" disabled selected>Interested In</option>
                    <option value="male">Male</option>
                    <option value="female">Female</option>
                </select>
            </div>

            <input type="text" name="genres" placeholder="Interests (coding, music, art...)" class="w-full bg-black border border-white/10 p-4 rounded-2xl outline-none focus:border-rose-500">
            
            <button type="submit" class="w-full bg-white text-black py-4 rounded-2xl font-black uppercase text-xs hover:bg-rose-500 hover:text-white transition shadow-xl">
                Get Started
            </button>
        </form>
        <p class="text-center mt-6 text-gray-500 text-xs font-bold uppercase tracking-widest">
            Already have a vibe? <a href="/login" class="text-white hover:text-rose-500">Login</a>
        </p>
    </div>
</body>
</html>